# Upgrade Plan — 2026-05-08
> Source: `.cursorsession` sem `beesy_version` / `decisions_applied_until` -> installed `4.16.18-beta`
> Decisions scanned: 68 (`#31..#178`, baseline ausente tratado como `0`)
> Generated by: `/jarvis upgrade`
> Status: Proposed

## Current State

- Branch: `sprint/D2`
- Working tree: clean
- Local unpushed commit: `b411017 fix(sprint-F1): close D2 validation blockers`
- `checkpoint.phase`: `done` (Sprint Active Gate aprovado)
- `sprint_atual`: `F1`
- `validation_gate.result`: `APROVADA`
- `beesy_version`: absent
- `decisions_applied_until`: absent
- `strategic_context_captured_at`: absent
- Installed Beesy: `4.16.18-beta`

## Target State

- `beesy_version`: `4.16.18-beta`
- `decisions_applied_until`: `178`
- `strategic_context_captured_at`: `2026-05-08T19:30:00-03:00`
- `contexto_rapido`: append a short note that `/jarvis upgrade` aligned framework metadata after F1 approval.

## Plan

### Decision #64 — Entrevista Estratégica Obrigatória no Migrar

- **Category**: C
- **Status for this project**: OK by existing evidence, but metadata missing.
- **Evidence**: `.cursorsession` already contains product/user context (`usuarios_alvo`, `principal_preocupacao`, modules, pending features, technical decisions) and the project has planning docs (`docs/REQUIREMENTS.md`, `docs/ARCHITECTURE.md`, `docs/CONTRACTS.md`, `docs/INFRASTRUCTURE.md`, `docs/ROADMAP.md`).
- **Required action**: set `strategic_context_captured_at` to record that strategic context is already captured.
- **Risk**: LOW.

### Decision #164 — Unified Upgrade Command + Orchestrator Agent

- **Category**: C
- **Status for this project**: DRIFT.
- **Evidence**: `.cursorsession` lacks `beesy_version`, `decisions_applied_until`, and `strategic_context_captured_at`.
- **Required action**: update `.cursorsession` with the framework metadata above after plan approval.
- **Files affected**: `.cursorsession`.
- **Risk**: LOW.

### Decision #172 — `/jarvis upgrade` Hardening

- **Category**: C
- **Status for this project**: OK.
- **Evidence**: Sprint Active Gate was evaluated before planning. `checkpoint.phase=done`, `validation_gate.result=APROVADA`, and there are no open blockers.
- **Required action**: no code change; keep this plan as audit evidence.
- **Risk**: LOW.

### Decisions #177 and #178 — Latest Framework Changes

- **Category**: framework/tooling.
- **Status for this project**: N/A for project code.
- **Evidence**: #177 changes framework validation behavior; #178 changes framework installer file copy behavior.
- **Required action**: no project code change. Bump `decisions_applied_until` only after accepting this plan.
- **Risk**: LOW.

## Deferred / Not Auto-Fixed

- `.cursorsession.cenario = "ANALISE"` and `.cursorsession.perfil = "shell"` still fail the current Beesy validator schema, which expects `cenario` in `{NOVO, LEGADO, PARCIAL}` and `perfil` in `{nestjs, laravel, flutter, framework}`.
- This project is intentionally a Bash/Shell repository. The upgrade will not rewrite `perfil` to an inaccurate value. Treat this as a framework schema gap or handle in a separate PMO/framework decision.

## Rollback

- Restore `.cursorsession` from the backup created before applying:
  - `.cursorsession.bak-YYYY-MM-DD-HHMM`
- Or revert the metadata-only commit if the upgrade is committed separately.

## Apply Steps

1. Back up `.cursorsession`.
2. Add `beesy_version`, `decisions_applied_until`, and `strategic_context_captured_at`.
3. Append a short upgrade note to `contexto_rapido`.
4. Validate `.cursorsession` JSON syntax.
5. Leave schema drift for `perfil=shell` documented rather than masking it.
